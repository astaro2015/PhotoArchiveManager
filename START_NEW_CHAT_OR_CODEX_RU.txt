Photo Archive Manager — передача проекта в новый чат / Codex

Каноническая база: 1.15.7.
1.11.0 добавляет быструю безопасную раскладку по году/месяцу/дню поверх 1.10.2; отвергнутая 1.6.2 не должна использоваться как база или источник XAML.

Стартовый запрос:
«Продолжаем Photo Archive Manager. Этот ZIP — каноническая версия 1.15.7. Сначала прочитай HANDOFF_NEW_CHAT_OR_CODEX_RU.md, README_RU.md, NEXT_STAGE.txt и VERSION.txt, затем изучи актуальный код. Версия 1.6.2 была отвергнута — не используй её. Не начинай архитектуру заново. Работай прямо с текущим проектом, сохрани safety-инварианты, совместимость существующей Data/archive.db, SQLite-миграции и self-contained single-EXE сборку. После изменений верни полный обновлённый source ZIP.»

Ключевое текущее состояние:
- версия 1.15.7; Quality Score v3.3 / QualityAlgorithmVersion=6;
- compile hotfix 1.15.7: исправлен CS0136 в batch drag&drop групп людей;
- build hotfix 1.15.6: NU1900 (недоступны vulnerability data NuGet) остаётся warning и не блокирует restore; NU1901-NU1904 остаются errors;
- в «Похожие / копии» и «Сериях» quarantine-галочки накапливаются по всей вкладке; финальная команда обрабатывает все отмеченные группы/серии, а reload/quality refresh не теряет marks других групп;
- в «Людях» Ctrl/Shift/Ctrl+A работают и для групп, и для лиц: группы можно объединять/расформировывать/исключать и drag&drop-объединять пачкой; лица можно назначать/убирать/исключать и перетаскивать пачкой; drop лиц/групп на «Без группы» выполняет batch unassign/dissolve;
- в «Событиях» несколько выделенных фото переносятся drag&drop одной пачкой; после переноса PAM остаётся в source, пока оно не опустело;
- в «Точных дублях → По папкам» список состоит из уникальных пар A ↔ B; в карточках явно выбирается A или B, а единственная action-кнопка убирает exact-дубли только из выбранной стороны текущей пары;
- во всех групповых разделах («Точные дубли», «Похожие / копии», «Серии», «Люди», «События») удалён отдельный мини-review `‹ / Обработано / ›`; группы выбираются напрямую в списках слева;
- есть фильтр уникальных пар, где текущая пара покрывает все найденные exact-дубли хотя бы одной стороны; уникальные файлы не затрагиваются;
- отдельный «Мастер разбора…» точных дублей удалён в 1.12.1 как дублирующий workflow;
- Организация имеет быстрые режимы `год`, `год → месяц`, `год → месяц → день` и прежний полный режим `год → месяц/событие`;
- быстрые режимы сохраняют исходные имена; месяцы именуются `01 — январь` … `12 — декабрь`;
- после быстрой раскладки PAM может сразу переключиться на полный режим и построить новый Preview в том же корне, не запуская второй перенос автоматически;
- дополнительные копии фото не создаются; физические перемещения по-прежнему защищены SHA-256, запретом overwrite и Undo;
- SQLite user_version=20; existing Data/archive.db открывается/мигрирует на месте;
- рейтинг и «Избранное» убраны из рабочей логики; новая БД эти колонки не создаёт, в старой они остаются нетронутыми только как legacy-данные;
- Библиотека имеет «Требуют разбора»/отдельные проблемные статусы и сортировку Дата ↓/↑, Имя, Путь;
- обложки без ручного выбора предпочитают Quality Score;
- вероятно закрытые глаза оцениваются локальной OpenCV-эвристикой только для основных лиц YuNet;
- OpenCvSharpExtern/YuNet/SFace проверяются SHA-256; runtime-интернет для анализа отсутствует;
- build сохраняет portable Data через проверяемый recovery backup и запускает final-EXE portable self-test;
- исходные фотографии read-only по умолчанию; safety workflows Organization/Quarantine не ослаблять.

Разработчик: Привалов Олег; версия: 1.15.7; дата создания проекта: 01.09.2026.
